'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
export default function AdminPage() {
  const router = useRouter(); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [loading, setLoading] = useState(false);
  async function submit(event: FormEvent) { event.preventDefault(); setLoading(true); setError(''); const response = await fetch('/api/admin/session', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email, password }) }); const data = await response.json(); if (!response.ok) { setError(data.error || 'Não foi possível entrar.'); setLoading(false); return; } router.push('/dashboard'); }
  return <main className="min-h-screen bg-[#0e1217] px-4 py-8 text-slate-50"><div className="mx-auto max-w-md"><a href="/" className="mb-6 inline-block text-sm text-slate-400">← Voltar ao site</a><section className="rounded-3xl border border-slate-700 bg-[#141a20] p-6"><p className="text-xs uppercase tracking-[.25em] text-emerald-300">NEXORA PAY</p><h1 className="mt-2 text-2xl font-semibold">Acesso administrativo</h1><form onSubmit={submit} className="mt-7 space-y-5"><label className="block text-sm text-slate-300">E-mail<input value={email} onChange={(e) => setEmail(e.target.value)} type="email" autoComplete="username" required className="mt-2 w-full rounded-xl border border-slate-700 bg-[#10161c] px-4 py-3 text-white" /></label><label className="block text-sm text-slate-300">Senha<input value={password} onChange={(e) => setPassword(e.target.value)} type="password" autoComplete="current-password" required className="mt-2 w-full rounded-xl border border-slate-700 bg-[#10161c] px-4 py-3 text-white" /></label>{error && <p role="alert" className="rounded-xl border border-red-500/30 bg-red-500/10 p-3 text-sm text-red-300">{error}</p>}<button disabled={loading} className="w-full rounded-xl bg-emerald-600 px-4 py-3 font-semibold disabled:opacity-60">{loading ? 'Entrando...' : 'Entrar no painel'}</button></form><p className="mt-6 text-xs text-slate-500">As credenciais são definidas pelas variáveis ADMIN_EMAIL e ADMIN_PASSWORD da Vercel.</p></section></div></main>;
}
